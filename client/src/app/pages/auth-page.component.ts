import {ChangeDetectionStrategy, Component, EventEmitter, Input, Output} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms';
export interface AuthenticationSubmission {username:string; email:string; password:string; confirmation:string;}
@Component({selector:'ft-auth-page',standalone:true,imports:[CommonModule,FormsModule],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./auth-page.component.html'})
export class AuthPageComponent { @Input({required:true}) mode:'login'|'register'='login'; @Input() loading=false; @Output() modeChange=new EventEmitter<'login'|'register'>(); @Output() submitAuthentication=new EventEmitter<AuthenticationSubmission>(); username=''; email=''; password=''; confirmation=''; submit():void {this.submitAuthentication.emit({username:this.username,email:this.email,password:this.password,confirmation:this.confirmation});} toggle():void {this.modeChange.emit(this.mode === 'login' ? 'register' : 'login'); this.password='';this.confirmation='';} }
